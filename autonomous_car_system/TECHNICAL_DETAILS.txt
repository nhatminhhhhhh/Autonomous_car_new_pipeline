================================================================================
  AUTONOMOUS CAR SYSTEM — Technical Details, Algorithms & Pipeline
================================================================================

1. SYSTEM OVERVIEW
--------------------------------------------------------------------------------
The autonomous car system uses a camera-based perception-to-control pipeline
running on a Jetson Nano (with Windows fallback for development). It consists
of two deep learning models running in sequence:

   Camera Frame
       |
       v
   [RoadSegNet]  ->  road segmentation mask (3 classes: background, road, lane)
       |
       v
   [DrivingNet]  ->  steering angle prediction (-1.0 to +1.0)
       |
       v
   [PWMController]  ->  servo motor commands

The system supports three operating modes:
  - test:        Real-time road segmentation visualization only
  - collect:     Teleoperation data collection (WASD control)
  - autonomous:  Full end-to-end autonomous driving


2. DIRECTORY STRUCTURE
--------------------------------------------------------------------------------
autonomous_car_system/
  configs/config.py          Central configuration & hyperparameters
  models/
    road_segnet.py            RoadSegNet architecture (U-Net variant)
    driving_net.py            DrivingNet architecture (NVIDIA PilotNet)
  data/
    dataset.py                PyTorch Dataset classes + data augmentations
    prepare_dataset.py        Converts JSON polygon annotations to segmentation masks
    teleoperation.py          Keyboard-controlled data collection tool
  hardware/
    camera.py                 Platform-aware camera + threaded capture
    pwm_controller.py         PWM servo control + safety monitor
  inference/
    road_detector.py          RoadSegNet inference wrapper
    driving_predictor.py      DrivingNet inference wrapper
    pipeline.py               Combined autonomous pipeline class
  training/
    train_road.py              RoadSegNet training script
    train_driving.py           DrivingNet training script
    export_trt.py              TensorRT export script
  scripts/
    run.py                    Main entry point (test/collect/autonomous modes)
    collect_data.py           CLI for data collection
    fps_diagnostic.py          Camera FPS benchmarking tool
  utils/
    visualization.py           Prediction visualization (saves PNG plots)
    model_utils.py             Checkpoint save/load helpers
    metrics.py                 IoU calculation, loss plotting


3. MODELS
--------------------------------------------------------------------------------

3a. RoadSegNet (road_segnet.py)
  - Architecture: Lightweight encoder-decoder (U-Net variant)
  - Encoder: 4 stages of SeparableDoubleConv + MaxPool2d (2x)
      features = [8, 16, 32, 64]  (channel depths)
  - Bottleneck: SeparableDoubleConv(64, 128)
  - Decoder: 4 stages of Upsample(bilinear, 2x) + skip connections + conv
  - Output: 1x1 Conv2d(features[0], num_classes)  ->  pixel-wise logits
  - Key design choices:
      * SeparableConv2d (depthwise + pointwise) reduces parameters vs standard conv
      * Dropout2d(0.2) after each decoder block for regularization
      * Input: 128x128 RGB, Output: 128x128 class indices (3 channels)
  - Parameter count: ~95K (very lightweight for embedded deployment)

3b. DrivingNet (driving_net.py)
  - Architecture: NVIDIA PilotNet-inspired end-to-end steering regressor
  - Convolutional layers:
      Conv2d(3, 24, 5x5, stride=2, pad=2)  + BN + ReLU
      Conv2d(24, 36, 5x5, stride=2, pad=2) + BN + ReLU
      Conv2d(36, 48, 5x5, stride=2, pad=2) + BN + ReLU
      Conv2d(48, 64, 3x3, stride=1, pad=1) + BN + ReLU
      Conv2d(64, 64, 3x3, stride=1, pad=1) + BN + ReLU
  - Fully-connected layers:
      Flatten -> Linear(flattened_size, 100) -> ReLU -> Dropout(0.5)
              -> Linear(100, 50) -> ReLU -> Dropout(0.5)
              -> Linear(50, 10) -> ReLU
              -> Linear(10, 1) -> steering scalar
  - Input: 66x200 RGB (PilotNet crop-friendly aspect ratio)
  - Output: Single float (steering angle, clamped to [-1.0, 1.0])
  - Flattened size computed dynamically via a dummy forward pass


4. TRAINING PIPELINE
--------------------------------------------------------------------------------

4a. RoadSegNet Training (train_road.py)
  - Loss: CrossEntropyLoss (pixel-wise classification)
  - Optimizer: Adam (lr=5e-3, weight_decay=5e-5)
  - Scheduler: ReduceLROnPlateau (factor=0.5, patience=10)
  - Early stopping: patience=15 epochs on validation loss
  - Batch size: 16
  - Image size: 128x128
  - Data augmentation (via albumentations):
      * HorizontalFlip (p=0.5)
      * RandomBrightnessContrast (p=0.3)
      * GaussNoise (p=0.2)
      * Affine scale/translate (p=0.3)
  - Checkpoints saved every 10 epochs + best model by val loss
  - Post-training: loss plot + prediction visualization saved

4b. DrivingNet Training (train_driving.py)
  - Loss: MSELoss (regression to steering angle)
  - Optimizer: Adam (lr=1e-4)
  - Scheduler: ReduceLROnPlateau (factor=0.5, patience=5)
  - Early stopping: patience=15 epochs on validation MSE
  - Batch size: 32
  - Image size: 66x200
  - Data: JSON-labeled pairs (image_path, steering, throttle)
  - Train/val split: 85%/15% random split
  - Post-training: scatter plot of predictions vs ground truth + error histogram

4c. TensorRT Export (export_trt.py)
  - Converts trained PyTorch models to TensorRT FP16 engines
  - Uses torch2trt library
  - Both RoadSegNet and DrivingNet are exported
  - Runtime: inference module checks for _trt.pth suffix and loads TRT if available


5. INFERENCE PIPELINE
--------------------------------------------------------------------------------

5a. RoadDetector (inference/road_detector.py)
  - Loads RoadSegNet (PyTorch or TensorRT)
  - Preprocessing: resize to 128x128, RGB conversion, normalize (ImageNet stats)
  - Inference: single forward pass, argmax over classes
  - Postprocessing: nearest-neighbor resize back to original frame size
  - Color LUT: [0,0,0] = background, [0,200,0] = road (green), [0,220,220] = lane (cyan)
  - FP16 inference when CUDA available

5b. DrivingPredictor (inference/driving_predictor.py)
  - Loads DrivingNet (PyTorch or TensorRT), gracefully handles missing checkpoint
  - Preprocessing: resize to 66x200, RGB conversion, normalize (ImageNet stats)
  - Output: single float clamped to [-1.0, 1.0]
  - If model is None (no checkpoint found), returns 0.0 (straight)

5c. AutonomousPipeline (inference/pipeline.py)
  - Combines RoadDetector + DrivingPredictor + CameraThread
  - Main loop: reads frames (non-blocking), runs inference, displays overlay
  - FPS counter for performance monitoring


6. HARDWARE INTERFACE
--------------------------------------------------------------------------------

6a. Camera (hardware/camera.py)
  - Platform detection via /etc/nv_tegra_release and /proc/device-tree/model
  - Jetson Nano: IMX219 camera via GStreamer pipeline (1640x1232 capture, 640x480 display)
      "nvarguscamerasrc ! video/x-raw(memory:NVMM) ! nvvidconv ! videoconvert ! appsink"
  - Windows: DirectShow (DSHOW) or Media Foundation (MSMF) backends
  - CameraThread: background thread with bounded queue (maxsize=2), drops stale frames

6b. PWM Controller (hardware/pwm_controller.py)
  - Abstraction over wiringpi/Jetson.GPIO with simulation fallback
  - Steering: maps [-1.0, 1.0] to pulse width (center=90, range=45)
  - Throttle: maps [-1.0, 1.0] to pulse width (min=60, max=180)
  - SafetyMonitor: automatic timeout (2s) + emergency stop
  - Simulation mode: prints PWM values when hardware not available


7. DATA COLLECTION
--------------------------------------------------------------------------------
  - TeleoperationCollector (data/teleoperation.py):
      * WASD keyboard control (pynput listener)
      * A/D = steering, W/S = throttle, Space = brake, R = toggle recording
      * Saves synchronized frame.jpg + frame.json (steering, throttle, timestamp)
  - PrepareDataset (data/prepare_dataset.py):
      * Converts LabelMe JSON polygon annotations to PNG segmentation masks
      * Supports multiple label classes via labels.txt
      * Splits data into train/val (80/20)
      * Output: {train,val}/{images,labels}/


8. CONFIGURATION (configs/config.py)
--------------------------------------------------------------------------------
  - device:            CUDA if available, else CPU
  - RoadSegNet:        3 classes, 128x128 input, 16 batch, 200 epochs, lr=5e-3
  - DrivingNet:        66x200 input, 15 steering bins, 100 epochs, lr=1e-4
  - Camera:            IMX219 @ 1640x1232 capture, 640x480 display, 30fps
  - PWM:               50Hz, steering center=90, range=45, throttle 60-180
  - Paths:             checkpoints/, data/collected_driving/


9. DEPENDENCIES (requirements.txt)
--------------------------------------------------------------------------------
  torch, torchvision, opencv-python, numpy, albumentations,
  matplotlib, tqdm, pynput
  (optional on Jetson: torch2trt, Jetson.GPIO, wiringpi)
